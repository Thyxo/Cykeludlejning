{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "npm run db:push && npm run build && npm run start",
    "healthcheckPath": "/health"
  }
}
